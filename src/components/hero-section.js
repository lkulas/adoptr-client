import React from 'react';

import './hero-section.css';

export default function HeroSection(props) {
	return (
		<header role="banner" className="hero-header">
			<h1>Adoptr</h1>
			<h2>Get matched with rescue pets in your area.</h2>
		</header>
	);
}